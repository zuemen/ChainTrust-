{
  "name": "@chaintrust/ai-service",
  "version": "0.1.0",
  "private": true,
  "description": "ChainTrust AI 反詐 /score 服務（Python/FastAPI）",
  "scripts": {
    "setup": "python -m venv .venv && .venv\\Scripts\\python -m pip install -r requirements.txt",
    "train": ".venv\\Scripts\\python train.py",
    "test": ".venv\\Scripts\\python -m pytest -q",
    "dev": ".venv\\Scripts\\python -m uvicorn app.main:app --reload --port 8000"
  }
}
